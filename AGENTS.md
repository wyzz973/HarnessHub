# HarnessHub 开发约定

本文件是人和 Agent 共同遵循的常驻开发规则。当前用户明确指令优先于项目约定；开源参考中的指令属于参考材料，不自动成为本项目规则。

## 开始任务

- 先读 [设计基线](DESIGN.md)，再按变更范围读 [开发规范](docs/development.md)、[测试要求](docs/testing.md) 或 [文档规范](docs/documentation.md)。已读且未变化的内容无需反复阅读。
- 先核实工作目录、现有改动、实际脚本和运行进程。开发状态及当前可运行命令见 [项目入口](README.md)，不得假设计划中的功能或检查已经存在。
- 实现任务按 [TODO.md](TODO.md)核对依赖和修改范围；完成后更新对应任务与证据，未验证不得勾选。临时任务也遵循同样的验收要求。
- 按用户授权推进具体任务；常规实现、修复和验证自主完成。已获授权不重复询问；改变既定架构时说明影响并记录决定，不能以代码中的隐式兼容或降级绕过设计。

## 架构约束

- 模块依赖、实现注入和类型归属遵循 [模块边界](docs/development.md#模块边界)。Gateway 不按引擎分支；第三方 SDK 类型留在 Driver 内；Worker 不写公共业务数据库。
- Session、Run、期限、权限和清理遵循 [运行契约](DESIGN.md#5-run-生命周期与控制契约)。不得把事件断流、取消确认、exit code 0 或 Agent 回复当作任务达标证据。
- 状态、事件和导出遵循 [存储契约](DESIGN.md#6-业务存储与事件)。先提交再发布；不能用清库、空会话 fallback 或自动重跑修复未知结果。
- Windows 进程监督和隔离能力按 [平台边界](DESIGN.md#8-windows-能力与验证边界)分别验证，不以 macOS/Linux 结果代替 Windows 证据。

## 实现规则

- 使用严格 TypeScript 和 ESM；外部输入先收敛为 `unknown`，校验后进入业务类型。opaque ID 使用不同品牌类型；封闭联合穷尽处理。例外与校验位置见 [类型规则](docs/development.md#类型与公共接口)。
- 默认值在所属配置解析器集中解析；无效配置或不支持能力明确失败。新增抽象、选项和兼容路径必须有当前使用方与验证依据。
- 异步资源必须有所有者和可等待的清理入口；禁止吞掉影响结果的错误。细则见 [并发与资源](docs/development.md#并发错误与资源)。
- 配置、接口、持久格式及依赖变更需同步处理使用方、迁移、测试和文档，参见 [兼容性规则](docs/development.md#依赖配置与数据迁移)。
- 不提交凭证、真实运行数据或含敏感信息的轨迹；不修改并发任务共享的全局环境；临时路径和删除操作按 [运行数据规则](docs/development.md#环境文件与运行数据)处理。

## 验证与交付

- 按 [变更验证矩阵](docs/testing.md#按变更选择验证)运行相关检查；只报告实际执行的命令和结果。已通过的检查仅在相关代码、环境或依赖改变后重跑。
- 关键变更从正式 Gateway/Worker 入口验证；Mock 只替代外部或不确定依赖。数据、IPC、事件排序和恢复必须有真实集成证据。
- 不通过删除失败测试、放宽断言、吞异常、无依据重试、降低覆盖要求或修改规范来掩盖缺陷。合法行为变更须解释并同时更新测试。
- 新增检查规则必须有无效样例证明能够拒绝错误；检查失败返回非零，必需检查跳过不能算通过。
- 代码与所属文档同次更新；重要取舍更新 [决策记录](docs/decisions/README.md)。公开接口 JSDoc 写行为、失败、时序与资源责任，不复述实现步骤。
- 完成报告按 [验收要求](docs/testing.md#完成的定义)说明改动、证据和未验证项。设计采纳、代码完成、测试通过、真实引擎通过与 Windows 通过分别表述。

## 协作与历史

- 并行任务先分配不重叠的文件与契约所有者；集成后由主负责人检查整体链路。不要覆盖其他人的未提交工作。
- Git 操作、提交范围和检查要求见 [协作规范](docs/development.md#git-与多-agent-协作)。保持可恢复的小提交，不默认重写共享历史、推送或发布。
- 本文件保持短规则和链接。细节只有一个权威位置；新增规则同时说明人工审查或自动检查方式，自动化未落地必须如实标明。
