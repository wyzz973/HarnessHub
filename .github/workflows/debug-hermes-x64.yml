name: Debug Hermes terminal on Windows x64

# Debug-only workflow for the Hermes terminal-tool hang in the released x64 competition
# bundle. It needs no model key: Hermes is driven directly or through the scripted mock.
# Round 2: reproduce the stdin-inheritance hang without HarnessHub, apply
# scripts/prepare-hermes.mjs to the extracted bundle and prove the fix end to end.
on:
  push:
    branches:
      - ci/x64-debug-hermes

permissions:
  contents: read

jobs:
  hermes:
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install py-spy
        run: python -m pip install --disable-pip-version-check py-spy

      - name: Download and extract the released x64 competition bundle
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = "Stop"
          $zipName = "harnesshub-competition-full-windows-x64.zip"
          gh release download competition-latest --repo $env:GITHUB_REPOSITORY --pattern "$zipName*" --dir D:\dl
          if ($LASTEXITCODE -ne 0) { throw "download failed" }
          $zip = "D:\dl\$zipName"
          $expected = ((Get-Content -Raw -LiteralPath "$zip.sha256").Trim() -split '\s+')[0]
          $actual = (Get-FileHash -Algorithm SHA256 -LiteralPath $zip).Hash.ToLowerInvariant()
          if ($actual -ne $expected) { throw "ZIP SHA-256 mismatch: $actual" }
          & (Join-Path $env:ProgramFiles "7-Zip\7z.exe") x -y "-oD:\hh" $zip | Out-Null
          if ($LASTEXITCODE -ne 0) { throw "extract failed" }
          Remove-Item -LiteralPath $zip -Force
          New-Item -ItemType Directory -Force -Path D:\dbg | Out-Null
          "Bundle $actual" | Write-Host

      - name: Reproduce - bash probe with Hermes' pending-read stdin (no HarnessHub)
        run: |
          & D:\hh\engines\hermes\runtime\python.exe -I -B "$env:GITHUB_WORKSPACE\.github\scripts\hermes-debug\stdin_hang.py" D:\hh D:\dbg\stdin raw

      - name: Reproduce - Hermes LocalEnvironment with pending-read stdin (unpatched)
        run: |
          & D:\hh\engines\hermes\runtime\python.exe -I -B "$env:GITHUB_WORKSPACE\.github\scripts\hermes-debug\stdin_hang.py" D:\hh D:\dbg\stdin hermes-unpatched

      - name: Apply the product fix (scripts/prepare-hermes.mjs) to the extracted bundle
        run: |
          $ErrorActionPreference = "Stop"
          & D:\hh\runtime\node.exe scripts/prepare-hermes.mjs --runtime D:\hh\engines\hermes\runtime
          if ($LASTEXITCODE -ne 0) { throw "prepare-hermes failed" }
          & D:\hh\runtime\node.exe scripts/prepare-hermes.mjs --runtime D:\hh\engines\hermes\runtime
          if ($LASTEXITCODE -ne 0) { throw "prepare-hermes is not idempotent" }
          & D:\hh\engines\hermes\runtime\python.exe -I -B -c "import tools.environments.local as m, inspect; print('stdin probes:', inspect.getsource(m).count('stdin=subprocess.DEVNULL,'))"

      - name: Verify - Hermes LocalEnvironment with pending-read stdin (patched)
        run: |
          & D:\hh\engines\hermes\runtime\python.exe -I -B "$env:GITHUB_WORKSPACE\.github\scripts\hermes-debug\stdin_hang.py" D:\hh D:\dbg\stdin hermes-patched

      - name: Verify - Hermes through the competition Gateway with the scripted mock (patched)
        run: |
          New-Item -ItemType Directory -Force -Path D:\dbg\watch-gateway | Out-Null
          $p = Start-Process -FilePath D:\hh\runtime\node.exe -ArgumentList @("scripts/competition-matrix.mjs", "--bundle", "D:\hh", "--engines", "hermes", "--out", "D:\dbg\matrix", "--mock", "--engine-timeout-ms", "240000") -PassThru -NoNewWindow -RedirectStandardOutput D:\dbg\matrix.out.txt -RedirectStandardError D:\dbg\matrix.err.txt
          & "$env:GITHUB_WORKSPACE\.github\scripts\hermes-debug\watch.ps1" -Bundle D:\hh -Out D:\dbg\watch-gateway -Seconds 330 -WatchPid $p.Id
          if (-not $p.HasExited) { taskkill /PID $p.Id /T /F | Out-Null }
          Get-Content D:\dbg\matrix\summary.md -ErrorAction SilentlyContinue

      - name: Collect Hermes and Gateway logs
        if: always()
        run: |
          New-Item -ItemType Directory -Force -Path D:\dbg\state-logs | Out-Null
          Get-ChildItem D:\hh\state -Recurse -File -Include *.log -ErrorAction SilentlyContinue | ForEach-Object {
            Copy-Item -LiteralPath $_.FullName -Destination (Join-Path D:\dbg\state-logs (($_.FullName.Substring(6)) -replace '[\\:]', '__'))
          }

      - name: Upload debug evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hermes-debug-x64
          path: D:\dbg
          retention-days: 7
